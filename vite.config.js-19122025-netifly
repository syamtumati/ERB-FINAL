import { defineConfig } from "vite";
import react from "@vitejs/plugin-react-swc";

export default defineConfig(({ mode }) => ({
  plugins: [react()],
  server: {
    port: 5173,
  },

  // ✅ GitHub Pages vs Netlify
  base: mode === "github" ? "/ERB-FINAL/" : "/",
}));

