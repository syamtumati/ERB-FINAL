/* ================= ROOT ================= */
.home-page {
  min-height: 100vh;
  overflow-x: hidden;
  display: flex;
  flex-direction: column;
}

/* ================= HERO ================= */
.hero-video-section {
  position: relative;
  width: 100%;
  min-height: 100svh;
  overflow: hidden;
}

.hero-video {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.hero-overlay {
  position: relative;
  z-index: 1;
  min-height: 100svh;
  display: flex;
  justify-content: center;
  align-items: center;
  background: linear-gradient(
    to bottom,
    rgba(10, 8, 15, 0.2),
    rgba(15, 10, 30, 0.35)
  );
}

/* ================= HERO CONTENT ================= */
.hero-content-centered {
  width: 100%;
  max-width: 1100px;
  padding: 0 2rem;
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
}

/* ================= HERO TITLE ================= */
.hero-church-name {
  font-family: "Playfair Display", serif; /* 🔒 LOCKED FONT */
  font-size: 3.4rem;
  font-weight: 600;
  line-height: 1.2;
  letter-spacing: 0.02em;
  text-align: center;

  color: #d2b184;
  text-shadow: 0 2px 10px rgba(0, 0, 0, 0.35);

  /* 🔒 IDENTICAL RENDERING ON iOS / ANDROID */
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  text-rendering: optimizeLegibility;
}

/* Second line */
.hero-church-name .hero-baptist {
  display: block;
  font: inherit;
  color: inherit;
  text-shadow: inherit;
}

/* ================= VISIT BOX ================= */
.hero-visit-box {
  margin-top: 4rem;
  max-width: 520px;
  background: #f9f6ef;
  border: 2px solid #d7a955;
  border-radius: 18px;
  padding: 2.4rem;
  box-shadow: 0 22px 60px rgba(0, 0, 0, 0.55);
  color: #2b2620;
}

.hero-visit-title {
  font-size: 1.8rem;
  margin-bottom: 1.2rem;
}

.hero-visit-divider {
  display: flex;
  align-items: center;
  gap: 0.6rem;
  margin-bottom: 1.2rem;
}

.hero-visit-divider span {
  flex: 1;
  height: 2px;
  background: rgba(215, 169, 85, 0.6);
}

.hero-visit-icon {
  color: #d7a955;
}

.hero-visit-time,
.hero-visit-address {
  font-size: 1.1rem;
  line-height: 1.6;
}

/* ===================================================== */
/* MOBILE HERO */
/* ===================================================== */
@media (max-width: 768px) {
  .hero-church-name {
    font-size: 2.4rem;          /* size only */
    transform: translateY(-20px);
  }

  .hero-visit-box {
    margin-top: 9rem;
    padding: 1.2rem 1.4rem;
    max-width: 92%;
    border-radius: 10px;
  }
}

@media (max-width: 480px) {
  .hero-church-name {
    font-size: 2.05rem;         /* size only */
    transform: translateY(-20px);
  }

  .hero-visit-box {
    margin-top: 7.6rem;
  }
}

/* ===================================================== */
/* DESKTOP POSITIONING (THIS FIXES YOUR ISSUE) */
/* ===================================================== */
@media (min-width: 1024px) {
  .hero-overlay {
    justify-content: center;
    align-items: center;
  }

  .hero-content-centered {
    transform: translateY(-240px); /* ⬆️ REAL, VISIBLE MOVE */
  }
}

/* ===================================================== */
/* LAPTOP TUNING */
/* ===================================================== */
@media (min-width: 1024px) and (max-width: 1799px) {
  .hero-church-name,
  .hero-visit-box {
    margin-left: -40px;
  }

  .hero-visit-box {
    max-width: 420px;
    padding: 1.4rem 1.8rem;
    margin-top: 6.2rem;
    border-radius: 14px;
  }
}

/* ===================================================== */
/* LARGE DESKTOP */
/* ===================================================== */
@media (min-width: 1800px) {
  .hero-church-name,
  .hero-visit-box {
    margin-left: -35px;
  }

  .hero-visit-box {
    max-width: 520px;
    padding: 2.4rem 2.8rem;
    margin-top: 9rem;
  }
}

/* ================= WHAT MATTERS ================= */
.what-matters-section {
  background: #384852;
  padding: 4rem 1.5rem;
  text-align: center;
}

.container {
  max-width: 1350px;
  margin: 0 auto;
}

.main-title {
  font-size: 3.2rem;
  color: white;
  margin-bottom: 3rem;
}

.what-matters-grid {
  display: grid;
  grid-template-columns: repeat(2, 420px);
  gap: 3rem;
  justify-content: center;
}

@media (max-width: 768px) {
  .what-matters-grid {
    grid-template-columns: 1fr;
  }
}

