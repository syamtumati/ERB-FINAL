/* === BELIEF PAGE – NAVY BACKGROUND + CARDS === */

.belief-page,
.belief-page * {
  box-sizing: border-box;
}

.belief-page {
  min-height: 100vh;

  /* NAVY LINEN TEXTURE BACKGROUND */
  background:
    repeating-linear-gradient(
      0deg,
      rgba(5, 20, 45, 0.40),
      rgba(5, 20, 45, 0.40) 4px,
      rgba(3, 15, 35, 0.40) 4px,
      rgba(3, 15, 35, 0.40) 8px
    ),
    radial-gradient(circle at top, #000A1F 0%, #00142E 50%, #000818 100%);
  background-attachment: fixed;

  color: #EAF1FF;
  font-family: "Georgia", serif;

  display: flex;
  flex-direction: column;
  padding-top: 10rem;   /* base offset below navbar */
  overflow-x: hidden;
}

@supports (-webkit-touch-callout: none) {
  .belief-page {
    min-height: -webkit-fill-available;
  }
}

.belief-content {
  max-width: 1350px;
  margin: 0 auto;
  padding: 0 1.5rem;
  text-align: center;
  flex: 1;
}

/* HERO */

.belief-hero {
  max-width: 100%;
  margin: 0 auto;
}

.belief-hero h1,
.confession-title,
.tagline {
  text-align: center;
  margin-left: auto;
  margin-right: auto;
}

.belief-hero h1 {
  font-size: 4.2rem;
  font-weight: 900;
  color: #ffffff;
  margin: 0 0 1.5rem 0;
  letter-spacing: -1.5px;
  max-width: 90vw;
  overflow-wrap: break-word;
}

.confession-title {
  font-size: 2.3rem;
  font-weight: 700;
  color: #ffffff;
  margin: 0 0 1.2rem 0;
  max-width: 90vw;
}

.tagline {
  font-size: 2rem;
  font-weight: 600;
  color: #ffffff;
  margin: 0;
  line-height: 1.6;
  max-width: 90vw;
}

/* GRID – BASE (tablet/desktop) */

.confession-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
  gap: 2.8rem;
  margin: 5rem auto 3.5rem;
  max-width: 1300px;
  align-items: stretch;      /* make grid items match row height */
}

/* CARDS */

.confession-card {
  background: rgba(255, 255, 255, 0.88);
  backdrop-filter: blur(10px);
  padding: 2.4rem 2.1rem;
  border-radius: 2.4rem;
  box-shadow: 0 20px 60px rgba(0, 0, 0, 0.1);
  border: 1px solid rgba(139, 69, 19, 0.12);
  transition: transform 0.4s ease, box-shadow 0.4s ease;
  text-align: left;

  display: flex;
  flex-direction: column;
  height: 100%;
  min-height: 380px;          /* 🔹 equal visual height baseline */
}

.confession-card:hover {
  transform: translateY(-12px);
  box-shadow: 0 35px 80px rgba(0, 0, 0, 0.18);
}

.card-header {
  display: flex;
  align-items: center;
  gap: 0.9rem;
  margin-bottom: 1.1rem;
}

.chapter-num {
  font-size: 1.8rem;
  font-weight: 900;
  color: #6b4e31;
  background: rgba(107, 78, 49, 0.1);
  width: 48px;
  height: 48px;
  border-radius: 50%;
  display: grid;
  place-items: center;
  flex-shrink: 0;
}

.confession-card h3 {
  font-size: 1.6rem;
  color: #1a1208;
  margin: 0;
  font-weight: 800;
}

.confession-card p {
  font-size: 1.25rem;
  line-height: 1.8;
  color: #3a2a18;
  margin: 0;
  margin-top: 0.4rem;
}

/* BUTTON */

.belief-cta {
  margin-bottom: 3.5rem;
}

.coffee-btn {
  display: inline-block;
  padding: 1.6rem 4.5rem;
  background: linear-gradient(90deg, #6b4e31, #8c6b47);
  color: #ffffff;
  font-weight: 900;
  font-size: 1.5rem;
  text-transform: uppercase;
  letter-spacing: 2.2px;
  border-radius: 999px;
  text-decoration: none;
  box-shadow: 0 25px 70px rgba(107, 78, 49, 0.6);
  transition: all 0.4s;
  text-align: center;
  white-space: normal;
}

.coffee-btn:hover {
  transform: translateY(-12px);
  background: linear-gradient(90deg, #8c6b47, #a07d5b);
  box-shadow: 0 50px 110px rgba(107, 78, 49, 0.9);
}

/* ========== MOBILE & TABLET ========== */

@media (max-width: 768px) {
  .belief-page {
    padding-top: 7rem;
  }

  .belief-content {
    padding: 0 0.8rem;
  }

  .belief-hero {
    padding: 0 0.4rem;
  }

  .belief-hero h1 {
    font-size: 3.6rem;
    letter-spacing: -0.5px;
    max-width: 92vw;
  }

  .confession-title {
    font-size: 1.9rem;
    max-width: 92vw;
  }

  .tagline {
    font-size: 1.7rem;
    max-width: 92vw;
  }

  .confession-grid {
    grid-template-columns: 1fr;
    max-width: 100%;
    width: 100%;
    margin: 4.5rem 0 3rem;
    padding: 0;
    gap: 2rem;
    justify-items: center;
  }

  .confession-card {
    padding: 1.8rem 1.5rem;
    width: 100%;
    min-height: auto;           /* 🔹 mobile: cards grow naturally */
    height: auto;
    box-shadow: 0 14px 40px rgba(0,0,0,0.15);
  }

  .confession-card h3 {
    font-size: 1.5rem;
  }

  .confession-card p {
    font-size: 1.15rem;
    line-height: 1.8;
  }

  .belief-cta {
    display: flex;
    justify-content: center;
  }

  .coffee-btn {
    display: block;
    width: 100%;
    max-width: 320px;
    margin: 0 auto;
    padding: 1.3rem 1.1rem;
    font-size: 1.25rem;
    letter-spacing: 1.2px;
    line-height: 1.4;
    word-break: break-word;
  }
}

/* ========== DESKTOP OVERRIDES ========== */

@media (min-width: 1024px) {
  .confession-grid {
    max-width: 1200px;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 2.5rem;
  }

  .belief-hero {
    transform: translateX(-60px);
  }

  .belief-hero h1 {
    font-size: 3.6rem;
  }

  .belief-page {
    padding-top: 13rem;
  }

  .confession-card {
    min-height: 420px;          /* 🔹 nice tall uniform tiles on desktop */
  }

  .confession-grid,
  .belief-cta {
    transform: translateX(-10px);
  }
}

/* EXTRA SMALL (≤ 600px) */

@media (max-width: 600px) {
  .belief-page {
    padding-top: 7rem;
  }

  .belief-content {
    padding: 0 0.5rem;
  }

  .belief-hero {
    padding: 0 0.3rem;
  }

  .belief-hero h1 {
    font-size: 2.5rem;
    letter-spacing: 0;
    line-height: 1.2;
  }

  .confession-title {
    font-size: 1.6rem;
  }

  .tagline {
    font-size: 1.45rem;
  }

  .confession-card {
    padding: 1.6rem 1.3rem;
  }

  .coffee-btn {
    max-width: 300px;
    font-size: 1.2rem;
  }
}

/* === MOBILE FIX: MAKE ALL CARDS SAME HEIGHT === */
@media (max-width: 600px) {

  .confession-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2rem;
    align-items: stretch !important;   /* force equal height rows */
  }

  .confession-card {
    height: 100% !important;
    min-height: 380px !important;     /* uniform card height */
    display: flex;
    flex-direction: column;
    justify-content: flex-start;
  }

  .confession-card p {
    flex-grow: 1;                      /* push button/footer down */
  }
}

